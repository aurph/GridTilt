You have full context of the GridTilt codebase and what's currently live at gridtilt.com. I need you to build two features in sequence. Do Feature 1 first, then Feature 2. Do not skip steps. Ask me questions if anything is ambiguous about the current codebase before building.
FEATURE 1: Thesis Calculator Overhaul — Presets + Methodology Hardening
The problem: The Thesis Calculator currently works but feels like a blank spreadsheet. Users who don't already understand GW, capex per MW, and demand curves won't engage with it. The methodology also needs to be as rigorous and transparent as what we did when fixing the composite indices — no hand-wavy numbers, everything defensible.
What to build:
1. Three preset scenarios that users can select and then modify:

Conservative: 35 GW of new AI data center capacity added by 2030
Base Case: 50 GW added by 2030
Aggressive: 75 GW added by 2030

Each preset should auto-populate ALL input fields in the calculator with internally consistent assumptions. The user should be able to select a preset, see all fields populate, and then adjust any individual assumption.
2. The assumptions behind each preset must be sourced and defensible. Here's the framework — validate and adjust these numbers against what you know about the current data in the app:
For each scenario, the following variables should be populated:

Total new AI DC capacity (GW) by 2030: The headline number (35/50/75)
Average capex per MW of data center capacity: Use $7-12M/MW range. Conservative uses $10-12M (higher cost, tighter supply chain), Base uses $8-10M, Aggressive uses $7-9M (assumes economies of scale)
Generation mix for new power supply: What percentage comes from natural gas, nuclear (restarts + new), renewables + storage, and grid purchases. This should vary by scenario:

Conservative: 60% gas, 15% nuclear, 15% renewables, 10% grid
Base: 50% gas, 25% nuclear, 15% renewables, 10% grid
Aggressive: 40% gas, 35% nuclear, 15% renewables, 10% grid


Annual buildout pace (GW/year): Derived from total by 2030. Show the implied annual ramp.
Grid interconnection timeline assumption: Conservative = 4-5 year average, Base = 3-4 years, Aggressive = 2-3 years (assumes regulatory streamlining)
Implied transformer/switchgear demand: Derived from GW buildout × units per GW. This should output an estimated number of large power transformers (LPT) needed and compare against current US manufacturing capacity (~60 LPTs/year domestic).

3. Methodology transparency panel:
Add a collapsible "Methodology" section below or beside the calculator that explains:

Where each default number comes from (cite the source: IEA, EIA, McKinsey $5.2T projection, hyperscaler earnings calls, DOE grid studies)
How the output calculations work (show the formulas in plain English)
What the key sensitivities are (which inputs move the output the most)
A note that says: "All assumptions are adjustable. GridTilt provides the framework — you provide the thesis."

4. Output improvements:
The calculator outputs should include:

Total implied capex for the scenario (GW × $/MW)
Implied power generation buildout needed (broken down by type)
Transformer/grid hardware demand estimate with comparison to current manufacturing capacity
A simple visual: bar chart or similar showing the buildout timeline year by year (2025-2030)
Clear labeling: what's a GridTilt calculation vs. what's a user-adjustable input

5. UX requirements:

Preset buttons at the top: "Conservative | Base Case | Aggressive | Custom"
When a preset is selected, ALL fields populate and the outputs recalculate immediately
Any field the user changes should automatically switch the label to "Custom (modified from [preset name])"
Inputs should have sensible min/max ranges with validation (e.g., can't enter negative GW, can't have generation mix sum to more than 100%)
Mobile-responsive. The calculator should work on phones.
Match the existing GridTilt design system (colors, fonts, spacing)

6. Methodology standard:
Apply the same rigor we used when fixing the composite indices. Specifically:

No magic numbers. Every constant should trace back to a named source.
The math should be reproducible: a user who reads the methodology should be able to verify the outputs with a calculator.
If we're making an assumption, label it as an assumption and explain why we chose that default.
Test edge cases: what happens at 0 GW? At 200 GW? Do the outputs still make sense?


FEATURE 2: Power Map Filtering
The problem: The Power Map shows 41 AI data center facilities but they're all displayed at once with no way to explore subsets. Users can't answer basic questions like "where is Google building?" or "which facilities are in PJM?" without scanning the whole map.
What to build:
1. Filter panel (sidebar or top bar, your judgment on what fits the current layout):
Filters should include:

By operator: Google, Meta/Facebook, Amazon/AWS, Microsoft/Azure, Oracle, Other. Multi-select (can select multiple operators at once). Show facility count next to each.
By grid region/RTO: PJM, MISO, ERCOT, WECC, SERC, SPP, NPCC. Multi-select. Show facility count next to each.
By capacity range: Presets like <100 MW, 100-500 MW, 500+ MW. Or a slider if that fits better.
Clear all filters button that resets to full view.

2. Map behavior when filtered:

Filtered-out facilities should either disappear entirely or dim to ~20% opacity (whichever looks better — try both and pick). Don't just gray them out in a way that clutters the map.
The facility count should update dynamically: "Showing 8 of 41 facilities"
Grid Stress mode should still work alongside filters. If I filter to "Google only" and toggle Grid Stress, I should see Google facilities on the stress-colored map.
Zoom should auto-adjust to fit the filtered set if the user hasn't manually zoomed.

3. Click interaction:

Clicking a facility pin should show a detail card/popup with: facility name, operator, location, estimated capacity (MW), grid region/RTO, and status if we have it.
If we don't have all this data for every facility, show what we have and leave the rest blank rather than hiding the card.

4. URL state (nice to have):
If feasible, encode filter state in the URL query params so users can share a filtered view (e.g., gridtilt.com/power-map?operator=google&region=pjm). This makes the map shareable and is great for content — I can link to a specific filtered view in tweets.
5. Mobile:
Filters should collapse into a bottom sheet or hamburger on mobile. The map should be the primary view, not the filter panel.