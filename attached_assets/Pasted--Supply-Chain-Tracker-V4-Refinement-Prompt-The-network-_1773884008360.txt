# Supply Chain Tracker V4 — Refinement Prompt

The network graph layout is great. Keep it. This prompt ONLY fixes the data accuracy, adds missing connections, and cleans up visual issues. Do NOT rebuild the layout or change the D3 force approach.

---

## 1. DELETE THIS CONNECTION — IT'S WRONG

```
DELETE: Rare Earth → Solar
```

Solar panels are silicon-based. They do not use rare earth elements. This connection is factually incorrect. Remove it entirely.

---

## 2. FIX THESE CONNECTION LABELS

More precise labels based on what actually flows:

```typescript
// CORRECTED LABELS
{ source: 'uranium',     target: 'nuclear',       label: 'LEU fuel' },          // was: 'fuel'
{ source: 'uranium',     target: 'smrs',          label: 'HALEU fuel' },         // correct, keep
{ source: 'natural-gas', target: 'gas-turbines',  label: 'combustion fuel' },    // was: 'fuel'
{ source: 'steel',       target: 'transformers',  label: 'GOES cores' },         // was: 'cores' — GOES = grain-oriented electrical steel
{ source: 'steel',       target: 'line-const',    label: 'tower steel' },        // was: 'towers'
{ source: 'copper',      target: 'transformers',  label: 'winding copper' },     // was: 'windings'
{ source: 'copper',      target: 'hv-cable',      label: 'conductor' },          // correct, keep

// Generation → Transmission: these all mean the same thing — generator step-up
{ source: 'nuclear',     target: 'transformers',  label: 'GSU' },               // was: 'output' — GSU = Generator Step-Up transformer
{ source: 'gas-turbines',target: 'transformers',  label: 'GSU' },               // was: 'output'
{ source: 'solar',       target: 'transformers',  label: 'GSU' },               // was: 'output'
{ source: 'smrs',        target: 'transformers',  label: 'GSU' },               // was: 'output'

{ source: 'transformers',target: 'switchgear',    label: 'step-down' },          // was: 'stepped-down power'
{ source: 'transformers',target: 'power-mgmt',    label: 'facility feed' },      // was: 'facility power'
```

---

## 3. ADD THESE MISSING CONNECTIONS

These are real physical supply relationships that are currently missing. Without them, nodes on the left (Raw Materials) and right (End Use) sides of the graph appear disconnected.

```typescript
// ═══════════════════════════════════════════
// RAW MATERIALS → GENERATION (missing)
// ═══════════════════════════════════════════
{ source: 'steel',       target: 'nuclear',       label: 'reactor vessels' },    // nuclear reactor pressure vessels are specialized steel
{ source: 'steel',       target: 'gas-turbines',  label: 'turbine housing' },    // gas turbine casings are high-grade steel
{ source: 'copper',      target: 'nuclear',       label: 'plant wiring' },       // nuclear plants use massive amounts of copper wiring
{ source: 'copper',      target: 'gas-turbines',  label: 'generator windings' }, // generator coils inside gas turbines are copper
{ source: 'copper',      target: 'solar',         label: 'array wiring' },       // solar farms need copper wiring between panels

// ═══════════════════════════════════════════
// RAW MATERIALS → DISTRIBUTION (missing)
// ═══════════════════════════════════════════
{ source: 'copper',      target: 'cooling',       label: 'cooling pipes' },      // liquid cooling loops use copper tubing
{ source: 'copper',      target: 'dc-build',      label: 'facility wiring' },    // data centers need massive copper cabling
{ source: 'steel',       target: 'dc-build',      label: 'structural steel' },   // data center buildings are steel-framed

// ═══════════════════════════════════════════
// TRANSMISSION → DISTRIBUTION (missing)
// ═══════════════════════════════════════════
{ source: 'hv-cable',    target: 'dc-build',      label: 'site feed' },          // HV cable delivers power to the DC site
{ source: 'line-const',  target: 'hv-cable',      label: 'infrastructure' },     // line construction builds the routes that HV cable runs on

// ═══════════════════════════════════════════
// DISTRIBUTION → END USE (missing — these are critical)
// ═══════════════════════════════════════════
{ source: 'cooling',     target: 'miners',        label: 'thermal mgmt' },       // miners need cooling too
{ source: 'switchgear',  target: 'miners',        label: 'power routing' },      // miners need switchgear
{ source: 'power-mgmt',  target: 'dc-reits',      label: 'uptime' },             // DC REITs need UPS/backup
{ source: 'power-mgmt',  target: 'miners',        label: 'uptime' },             // miners need backup power

// ═══════════════════════════════════════════
// WITHIN END USE
// ═══════════════════════════════════════════
{ source: 'dc-reits',    target: 'compute',       label: 'colocation' },         // DC REITs house the compute hardware
{ source: 'hyperscalers',target: 'compute',       label: 'procurement' },        // hyperscalers buy the chips
```

---

## 4. REMOVE THIS CONNECTION — IT'S REDUNDANT/CONFUSING

```
DELETE: Nuclear → Line Construction (interconnect)
DELETE: Solar → HV Cable (interconnect)
```

These are vague. The real flow is: Generation → Transformers (GSU) → HV Cable → distribution. The generation-to-transformer connections already capture this. Having extra "interconnect" links muddies the graph without adding information.

---

## 5. FIX COMPANY COUNTS

Several nodes show wrong company counts. Update to match the actual tracked tickers:

```typescript
// Node ID → correct count
'uranium':      6,   // CCJ, UEC, NXE, DNN, UUUU, LEU
'copper':       4,   // FCX, SCCO, TECK, HBM
'steel':        4,   // NUE, STLD, CLF, X
'rare-earth':   2,   // MP, USAR
'natural-gas':  5,   // AR, EQT, RRC, SWN, LNG
'nuclear':      7,   // CEG, VST, TLN, SMR, OKLO, BWXT, SO
'gas-turbines': 4,   // GEV, SIEGY, BKR, NRG
'solar':        5,   // FSLR, ENPH, SEDG, NEE, AES
'smrs':         2,   // SMR, OKLO
'transformers': 4,   // ETN, ABB, EMR, HUBB
'hv-cable':     2,   // WIRE, NVT  ← currently shows "1 co." — WRONG
'line-const':   4,   // PWR, IDA, AYI, GNRC
'cooling':      3,   // VRT, CARR, JCI
'switchgear':   2,   // ETN, ABB
'dc-build':     7,   // EME, MTZ, STRL, FLR, J, ACM, PRIM
'power-mgmt':   2,   // VRT, GNRC
'hyperscalers': 5,   // META, MSFT, AMZN, GOOGL, AAPL
'dc-reits':     3,   // EQIX, DLR, AMT
'compute':      7,   // NVDA, AMD, AVGO, TSM, MU, INTC, SMCI
'miners':       3,   // IREN, CLSK, MARA
```

Make sure the count displayed under each node label matches this. Currently "HV Cable" shows "1 co." — should be "2 co." and "Rare Earth" shows "1 co." — should be "2 co."

---

## 6. COMPLETE UPDATED LINKS ARRAY

Here is the full, corrected links array. Replace the entire existing links array with this:

```typescript
const links: SupplyLink[] = [
  // ── RAW MATERIALS → GENERATION ──
  { source: 'uranium',     target: 'nuclear',       label: 'LEU fuel' },
  { source: 'uranium',     target: 'smrs',          label: 'HALEU fuel' },
  { source: 'natural-gas', target: 'gas-turbines',  label: 'combustion fuel' },
  { source: 'rare-earth',  target: 'nuclear',       label: 'generator magnets' },
  { source: 'steel',       target: 'nuclear',       label: 'reactor vessels' },
  { source: 'steel',       target: 'gas-turbines',  label: 'turbine housing' },
  { source: 'copper',      target: 'nuclear',       label: 'plant wiring' },
  { source: 'copper',      target: 'gas-turbines',  label: 'generator windings' },
  { source: 'copper',      target: 'solar',         label: 'array wiring' },

  // ── RAW MATERIALS → TRANSMISSION ──
  { source: 'copper',      target: 'transformers',  label: 'winding copper' },
  { source: 'copper',      target: 'hv-cable',      label: 'conductor' },
  { source: 'steel',       target: 'transformers',  label: 'GOES cores' },
  { source: 'steel',       target: 'line-const',    label: 'tower steel' },

  // ── RAW MATERIALS → DISTRIBUTION ──
  { source: 'copper',      target: 'switchgear',    label: 'busbars' },
  { source: 'copper',      target: 'cooling',       label: 'cooling pipes' },
  { source: 'copper',      target: 'dc-build',      label: 'facility wiring' },
  { source: 'steel',       target: 'dc-build',      label: 'structural steel' },

  // ── GENERATION → TRANSMISSION ──
  { source: 'nuclear',     target: 'transformers',  label: 'GSU' },
  { source: 'gas-turbines',target: 'transformers',  label: 'GSU' },
  { source: 'solar',       target: 'transformers',  label: 'GSU' },
  { source: 'smrs',        target: 'transformers',  label: 'GSU' },

  // ── TRANSMISSION → TRANSMISSION (internal) ──
  { source: 'line-const',  target: 'hv-cable',      label: 'infrastructure' },

  // ── TRANSMISSION → DISTRIBUTION ──
  { source: 'transformers',target: 'switchgear',    label: 'step-down' },
  { source: 'transformers',target: 'power-mgmt',    label: 'facility feed' },
  { source: 'hv-cable',    target: 'switchgear',    label: 'delivery' },
  { source: 'hv-cable',    target: 'dc-build',      label: 'site feed' },

  // ── DISTRIBUTION → END USE ──
  { source: 'cooling',     target: 'hyperscalers',  label: 'thermal mgmt' },
  { source: 'cooling',     target: 'dc-reits',      label: 'thermal mgmt' },
  { source: 'cooling',     target: 'compute',       label: 'chip cooling' },
  { source: 'cooling',     target: 'miners',        label: 'thermal mgmt' },
  { source: 'switchgear',  target: 'hyperscalers',  label: 'power routing' },
  { source: 'switchgear',  target: 'dc-reits',      label: 'power routing' },
  { source: 'switchgear',  target: 'miners',        label: 'power routing' },
  { source: 'dc-build',    target: 'hyperscalers',  label: 'facilities' },
  { source: 'dc-build',    target: 'dc-reits',      label: 'facilities' },
  { source: 'dc-build',    target: 'miners',        label: 'facilities' },
  { source: 'power-mgmt',  target: 'hyperscalers',  label: 'uptime' },
  { source: 'power-mgmt',  target: 'dc-reits',      label: 'uptime' },
  { source: 'power-mgmt',  target: 'compute',       label: 'uptime' },
  { source: 'power-mgmt',  target: 'miners',        label: 'uptime' },

  // ── WITHIN END USE ──
  { source: 'dc-reits',    target: 'compute',       label: 'colocation' },
  { source: 'hyperscalers',target: 'compute',       label: 'procurement' },
];
```

**Total: 42 connections** (up from 33). The graph should now show a densely connected web with no orphaned nodes.

---

## 7. VISUAL REFINEMENTS

### A. End Use nodes need to be connected

Right now the End Use column (Hyperscalers, DC REITs, Compute, Miners) looks disconnected — like a separate chart pasted on the right. With the new Distribution → End Use connections above, these nodes should now have visible incoming lines from Cooling, Switchgear, DC Build, and Power Mgmt. After adding the connections, verify that EVERY End Use node has at least 3 incoming lines.

### B. Copper should be the second-most connected node after Transformers

Copper connects to: Transformers, HV Cable, Switchgear, Nuclear, Gas Turbines, Solar, Cooling, DC Build — **8 connections**. It should be visually prominent in the graph, pulled slightly toward the center by the force simulation. If Copper appears isolated on the far left, increase its link force strength slightly so it drifts toward its many connected nodes.

### C. Node sizing by connection count

Scale node circle radius by number of connections. More connected = bigger circle. This makes Transformers (10+ connections) and Copper (8 connections) visually dominant, which correctly reflects their importance in the supply chain.

```typescript
const getNodeRadius = (nodeId: string) => {
  const connectionCount = links.filter(
    l => l.source === nodeId || l.target === nodeId
  ).length;
  // Scale from 28px (2 connections) to 42px (10+ connections)
  return Math.min(42, Math.max(28, 24 + connectionCount * 1.5));
};
```

### D. Connection line thickness by importance

The GSU connections (Generation → Transformers) are the highest-throughput links in the entire chain — all generated power flows through them. Make these slightly thicker (2px vs 1.5px for other links) so the "everything flows through Transformers" pattern is even more visually obvious.

### E. Label visibility

Connection labels ("GSU", "winding copper", "step-down", etc.) should be:
- Hidden by default at normal zoom
- Visible on hover over a connection line
- Visible when a connected node is clicked/selected
- Small (9px), monospace, with a dark background pill so they're readable over lines

This keeps the default view clean while making the labels available on interaction.

---

## 8. DETAIL PANEL — ADD "RECEIVES FROM" / "FEEDS INTO"

When clicking a node, the detail panel should show upstream and downstream connections. Generate these from the links array:

```typescript
const getUpstream = (nodeId: string) =>
  links.filter(l => l.target === nodeId).map(l => ({
    nodeId: l.source,
    nodeName: getNodeName(l.source),
    label: l.label,
  }));

const getDownstream = (nodeId: string) =>
  links.filter(l => l.source === nodeId).map(l => ({
    nodeId: l.target,
    nodeName: getNodeName(l.target),
    label: l.label,
  }));
```

Display in the detail panel:

```
RECEIVES FROM:
  Copper (winding copper) · Steel (GOES cores) · Nuclear (GSU) · Gas Turbines (GSU) · Solar (GSU) · SMRs (GSU)

FEEDS INTO:
  Switchgear (step-down) · Power Mgmt (facility feed)
```

Each node name in these lists should be clickable — clicking it selects that node and recenters the graph on it, showing ITS connections. This lets users walk through the supply chain node by node.

---

## EXECUTION

1. Replace the full links array with the corrected version above (Section 6)
2. Fix company counts (Section 5)
3. Add "RECEIVES FROM" / "FEEDS INTO" to the detail panel (Section 8)
4. Implement node sizing by connection count (Section 7C)
5. Test: verify every node has at least 2 connections, no orphans

Do NOT change the D3 force layout, the stage column headers, the node icon design, or the overall visual structure. The graph layout is correct — this prompt only fixes the data and interaction.